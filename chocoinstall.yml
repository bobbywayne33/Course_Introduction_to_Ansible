---
- name: win_chocolatey module demo
  hosts: all
  collections:
    -  chocolatey.chocolatey
  become: false
  gather_facts: false
  vars:
    - packages:
      - notepadplusplus
  tasks:
    - name: install packages
      win_chocolatey:
        name: "{{ packages }}"
        state: present
