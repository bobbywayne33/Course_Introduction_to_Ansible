---
- name: Get server facts
  hosts: all
  become: false
  tasks:
    - name: Get various server facts
      debug:
        msg:
          My machine hostname is {{ system_description }}
          and OS is {{ os_name }}
          and default IPV4 address is {{ ip_addresses }}
